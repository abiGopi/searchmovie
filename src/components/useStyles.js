import makeStyles from "@mui/styles/makeStyles";
const useStyles = makeStyles(() => ({
  searchItems: {
    padding: "150px 102px",
  },
  tableContent: {
    padding: "150px 102px",
  },
}));

export default useStyles;
